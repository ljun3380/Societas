spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/Characters_DB
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: iloveicode
  jpa:
    show-sql: true
    hibernate:
      naming.physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: update
